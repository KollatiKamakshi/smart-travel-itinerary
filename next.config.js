const nextConfig = {
  reactStrictMode: true,
  experimental: {
    appDir: true,
    optimizeCss: true,
  },
  // optional: disable offline for testing
  // output: 'standalone',
};

module.exports = nextConfig;
